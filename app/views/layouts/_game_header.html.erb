<nav class="navbar navbar-fixed-top game-header">
  <div class="container-fluid">
    <div class='left-nav'>
      <div id='my-turn' style='border: none'></div>
      <i class="fa" id='sidebar' aria-hidden="true" style='font-size: 2.5em'></i>
      <button class="btn btn-secondary btn-sm" id='endTurn' onclick="endTurn()"><strong>End Turn</strong></button>
    </div>
    <ul class='players nav-center' id='current-player'>
      <li>Current Player (<%=@game.turn_index%> of <%= @game.num_of_players %>)
      <h4><%= icon_for(@current_player, 25) %>
      <%= @current_player.user.name %>
      </h4></li>
    </ul>
    <ul class='players nav-center'>
      <li>Current Game (round <%=@game.round%>)<h4><%= @game.nick_name %></h4></li>
    </ul>

    <div class="navbar-default green" role='navigation'>
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>

      </div>
      <div class="collapse navbar-collapse" id='collapse'>
        <ul class="header_link nav navbar-nav pull-right">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <%= gravatar_for current_user, size: 30 %>
              <%= current_user.name %> <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Games", games_path %></li>
              <li><%= link_to "Help", help_path %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Exit Game", root_path %>
              </li>
            </ul>
          </li>
        </ul>
      </div>
  </div>
</nav>